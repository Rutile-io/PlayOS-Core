export default function getRandomInt(min: i32, max: i32): i32 {
    // const minf32 = F32.parseInt(min.toString());
    // const maxf32 = F32.parseInt(max.toString());


    // return Math.abs(Math.random() * (max - min + 1)) + min;
}
